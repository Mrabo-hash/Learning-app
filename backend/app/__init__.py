# backend/app/__init__.py

# This file marks the 'app' directory as a Python package.
# You can also use it to initialize package-level variables or imports if needed.

# Example: Import key modules to make them accessible as part of the app package
from .main import app
